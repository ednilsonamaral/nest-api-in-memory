// import { InMemoryDBEntity } from '@nestjs-addons/in-memory-db';

// export interface HeroesEntity extends InMemoryDBEntity {
//   marvelId: number;
// }

export interface HeroesEntity {
  marvelId: number;
}
